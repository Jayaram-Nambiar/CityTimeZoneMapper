# Optional environment variables
# Copy to .env for local overrides (never commit real secrets).

# Comma-separated browser origins, or * for open CORS (typical for public demos).
# CORS_ORIGINS=*

# Port used by the container / process managers (Dockerfile default: 8001).
# PORT=8001
